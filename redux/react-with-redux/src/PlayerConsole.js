export const PlayerConsole = () => {
    return <div>
        {/* 当点击这个button的时候，就能够fetch数据库中的所有歌单，并且列出来，点击每个song之后，
            可以增加很多的onClick 事件，就能够返还该歌曲的 id */}
        <button>Fetch Songs</button>

    </div>
}